#include <stdio.h>
/**
 *main- Entry point
 *
 *Return: 0 always (success)
 */
int main(void)
{
char ch;
 for (ch = 'a'; ch <= 'd'; ch = 'f'; ch <= 'p'; ch ch++)
putchar(ch);
for (ch = 'd'; ch <= ''; ch++)
putchar(ch);
putchar('\n');
return (0);
}
